[project]
name = "gestor"
version = "0.1.0"
description = "Gestor es una aplicación web para gestionar tareas y proyectos de manera eficiente."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1.3",
    "pymongo>=4.16.0",
]
